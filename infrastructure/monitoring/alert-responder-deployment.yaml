apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-responder
  namespace: monitoring
  labels:
    app.kubernetes.io/name: alert-responder
spec:
  # Single replica is intentional: alert handling is idempotent (Alertmanager
  # dedupes upstream) but parallel pod restart attempts could fight each
  # other. If we ever scale this it should become a leader-elected setup.
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: alert-responder
  template:
    metadata:
      labels:
        app.kubernetes.io/name: alert-responder
      annotations:
        reloader.stakater.com/auto: "true"
    spec:
      serviceAccountName: alert-responder
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: responder
          image: python:3.11-slim
          imagePullPolicy: IfNotPresent
          # Install the kubernetes client lib at startup. Avoids needing a
          # custom image / private registry. Installed to a writable
          # PYTHONUSERBASE under /tmp because the default ~/.local is on
          # the read-only root path for non-root users.
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              export PYTHONUSERBASE=/tmp/pypkgs
              mkdir -p "$PYTHONUSERBASE"
              pip install --user --quiet --no-cache-dir kubernetes==31.0.0
              exec python /app/responder.py
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: HOME
              value: /tmp
            - name: TELEGRAM_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: alert-responder-telegram
                  key: TELEGRAM_BOT_TOKEN
            - name: TELEGRAM_CHAT_ID
              valueFrom:
                secretKeyRef:
                  name: alert-responder-telegram
                  key: TELEGRAM_CHAT_ID
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            # Generous initial delay because pip install runs at startup.
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: code
              mountPath: /app
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # pip install writes to /root
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: code
          configMap:
            name: alert-responder-code
            defaultMode: 0444
---
apiVersion: v1
kind: Service
metadata:
  name: alert-responder
  namespace: monitoring
  labels:
    app.kubernetes.io/name: alert-responder
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: alert-responder
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
