apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencode-docker
  namespace: opencode
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
