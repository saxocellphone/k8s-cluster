# Public hostname via CF tunnel (DNS + wildcard → ingress-nginx).
# App auth is OpenCode HTTP Basic (OPENCODE_SERVER_PASSWORD) — works with
# `opencode attach -u … -p …` and browsers. Cloudflare Access is NOT used on
# this host (Access HTML breaks API clients; see PR for details).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencode
  namespace: opencode
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
spec:
  ingressClassName: nginx
  rules:
    - host: opencode.victornazzaro.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opencode
                port:
                  number: 4096
