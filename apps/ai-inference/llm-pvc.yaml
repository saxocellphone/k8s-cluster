apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llm-models
  namespace: ai-inference
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: longhorn
  resources:
    # HIPFire MQ weights + optional HF BF16 27B (~55–70Gi) + cache.
    requests:
      storage: 400Gi
