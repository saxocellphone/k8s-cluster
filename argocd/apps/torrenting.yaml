apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: torrenting
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/saxocellphone/k8s-cluster.git
    targetRevision: main
    path: apps/torrenting
  destination:
    server: https://kubernetes.default.svc
    namespace: torrenting
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
