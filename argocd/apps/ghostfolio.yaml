apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ghostfolio
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/saxocellphone/k8s-cluster.git
    targetRevision: main
    path: apps/ghostfolio
  destination:
    server: https://kubernetes.default.svc
    namespace: ghostfolio
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
